# MA24_SI-C1a3a_Evaluation202604_AttivonKodjo

"erreurs a la ligne 210 a 213 Ces surfaces sont créées au chargement du module, alors que pygame.init() n’est appelé que plus bas dans \_init(). ma24"

" erreurs a la ligne 242 246 250 254 Même problème : ces chargements se font avant l’appel à \_init() (qui contient pygame.init()).

" erreurs a la ligne 237 MA-24_pion noir le nom n'est pas le meme il y a un e en plus"

" erreurs a la ligne 152 153 ma24_dame_gfx.py la fonction selection ne fait rien.
